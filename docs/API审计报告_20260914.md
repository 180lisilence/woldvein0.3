====================================================================================
修改器内嵌 Lua -> 游戏 API 全量审计报告   (最终版)
共审计 58 个内嵌 Lua 脚本；对照游戏源码 1212 个类 / 297 个全局对象
====================================================================================

■ 结论
  调用与游戏源码匹配（且有对象可解析） : 101 处
  接收者是局部变量、但方法名在游戏中存在 : 93 处
  【严重】方法名在游戏任何类都不存在     : 0 个 []
  【严重】在已知对象上调用不存在的方法   : 0 组 []
  引用了游戏不存在的全局对象             : 24 个（见下，绝大多数是探查候选名/修改器自建标志）

■ 引用了游戏里不存在的全局对象明细
------------------------------------------------------------------------------------
  g_AchManager                     仅探查脚本的候选名尝试（无害）
  g_AchievementManager             仅探查脚本的候选名尝试（无害）
  g_BlockManager                   仅探查脚本的候选名尝试（无害）
  g_GameAchievementManager         仅探查脚本的候选名尝试（无害）
  g_LandManager                    仅探查脚本的候选名尝试（无害）
  g_MapManager                     仅探查脚本的候选名尝试（无害）
  g_PlotManager                    仅探查脚本的候选名尝试（无害）
  g_SteamAPI                       仅探查脚本的候选名尝试（无害）
  g_SteamAchievementManager        仅探查脚本的候选名尝试（无害）
  g_SteamManager                   仅探查脚本的候选名尝试（无害）
  g_TerrainManager                 仅探查脚本的候选名尝试（无害）
  g_TimeManager                    仅探查脚本的候选名尝试（无害）
  g_TimeMgr                        仅探查脚本的候选名尝试（无害）
  g_ZoneManager                    仅探查脚本的候选名尝试（无害）
  g_bCreativeMode                  修改器自建标志（非游戏对象，无害）
  g_cm_opt_infinite_resources      修改器自建标志（非游戏对象，无害）
  g_cm_opt_max_boom                修改器自建标志（非游戏对象，无害）
  g_cm_opt_unlimited_upgrade       修改器自建标志（非游戏对象，无害）
  g_cm_opt_unlock_buildings        修改器自建标志（非游戏对象，无害）
  g_trainer_base_delta             修改器自建标志（非游戏对象，无害）
  g_trainer_last_delta             修改器自建标志（非游戏对象，无害）
  g_trainer_lib_loaded             修改器自建标志（非游戏对象，无害）
  g_trainer_orig_GetCurSeason      修改器自建标志（非游戏对象，无害）
  g_trainer_orig_GetSeason         修改器自建标志（非游戏对象，无害）

■ 说明：本次已修复项（详见 CHANGELOG 2026-09-14）
------------------------------------------------------------------------------------
  ✓ g_FunctionManager:GetFucntionState  -> CheckFuncIsUnlock          (LUA_CREATIVE_ENABLE)
  ✓ g_blockMgr:GetCampBlockId           -> GetCampBlock():GetBlockID() (LUA_CREATIVE_ENABLE)
  ✓ g_GameWorld:IsPaused                -> g_Game:IsPaused()          (LUA_SIMWORLD_STATUS / TIME_DIAGNOSE)
  ✓ g_GameWorld:GetFrameCount           -> BuildingMgr:GetFrameTotalCount()
  ✓ g_GameWorld:GetName                 -> 已移除（LGameWorld 无此方法）
  ✓ g_GameWorld:RefreshSeason           -> GameWorldAudio:OnNewSeason() (LUA_TIME_SET_SEASON)
  ✓ boom:getBoom                        -> boom:GetBoom()             (3 处)
  ✓ camp:GetMoney                       -> camp:GetSourceValue(SOURCE.MONEY)
  ✓ camp:UpdatePopulation               -> g_LPopulationManager:_forceAllocatePopulation()
  ✓ 谋士候选名 g_AdvisorManager/g_AdviserManager -> g_LFamousAdviserMgr / g_LAdviserManager (2 处)